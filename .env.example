# Copy to .env and fill in. Read by docker-compose.yml for the pharmaassistant service.

# Required for real embeddings/generation.
OPENAI_API_KEY=

# Optional -- defaults shown are what docker-compose.yml falls back to.
LOG_LEVEL=INFO
ALLOWED_ORIGINS=
PRELOAD_TENANTS=client_1,client_2
